[tool.poetry]
name = "irina-finance-tools"
version = "0.1.0"
description = "Библиотека для финансовых расчётов: НДС, кредиты, счета"
authors = ["IrinaTom25 <iratom2014@gmail.com>"]
license = "MIT"
readme = "README.md"
packages = [{include = "irina_finance_tools", from = "src"}]

[tool.poetry.dependencies]
python = "^3.9"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"